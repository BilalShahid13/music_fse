/// Non-localizable string constants.
/// All user-visible text lives in ARB files and is accessed via
/// `AppLocalizations.of(context)!.keyName` — never here.
abstract final class UiStrings {
  static const String appName = 'Music FSE';
  static const String packageName = 'com.musicfse.player';
}
